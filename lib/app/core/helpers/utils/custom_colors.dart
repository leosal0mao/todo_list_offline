// NOTE site para pegar os nomes das cores
import 'package:flutter/material.dart';

class CustomColors {
  static Map<int, Color> appThemeColors = {
    100: const Color(0xFF390a3b),
  };

  static MaterialColor appTheme = MaterialColor(0xFF390a3b, appThemeColors);
}
